import React, { Component } from 'react';


const arrayBar = ( props ) => {
    return (
        <div className="array-bar" style={{height: `${props.bar}px`}}>
        </div>
    )
}

export default arrayBar;